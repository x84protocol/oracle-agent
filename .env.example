# ─── LLM Provider ──────────────────────────────────────────
# OpenAI API key (required — powers the oracle agent via @openai/agents)
OPENAI_API_KEY=sk-...

# Model to use (optional, defaults to gpt-4.1)
OPENAI_MODEL=gpt-4.1

# ─── Server ────────────────────────────────────────────────
# Port for the A2A server (optional, defaults to 4100)
PORT=4100

# Host to bind (optional, defaults to 0.0.0.0)
HOST=0.0.0.0

# ─── Agent Identity ───────────────────────────────────────
# Public URL where this agent is deployed (used in Agent Card)
AGENT_URL=http://localhost:4100

# x84 NFT mint address for this agent (optional — for on-chain identity)
AGENT_NFT_MINT=
